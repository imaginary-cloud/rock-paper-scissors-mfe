import { Moves } from "../components/play/Play.types";


export const createPlay = async ({
  gameId,
  move,
  npcMove,
}: {
  gameId: number,
  move: Moves,
  npcMove: Moves,
}): Promise<void> => {

}
